using AutoMapper;

namespace Integration.Orchestrator.Backend.Application.Helper
{
    public class MappingServerProfile : Profile
    {
        //public MappingServerProfile()
        //{
        //    CreateMap<ServerDto, ServerGetAllPaginated>()
        //    .ForMember(dest => dest.Id, opt => opt.MapFrom(src => src.id))
        //    .ForMember(dest => dest.Code, opt => opt.MapFrom(src => src.server_code))
        //    .ForMember(dest => dest.Name, opt => opt.MapFrom(src => src.server_name))
        //    .ForMember(dest => dest.TypeServerId, opt => opt.MapFrom(src => src.type_id))
        //    .ForMember(dest => dest.Url, opt => opt.MapFrom(src => src.server_url))
        //    .ForMember(dest => dest.StatusId, opt => opt.MapFrom(src => src.status_id))
        //    .ForMember(dest => dest.TypeServerName, opt => opt.MapFrom(src => src.TypeServerName));
        //}
    }
}
    
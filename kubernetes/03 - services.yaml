kind: Service
apiVersion: v1
metadata:
  name: svc-integration-orchestrator-backend
  namespace: ns-arquitectura-integracion-backend
spec:
  selector:
    app: group-integration-orchestrator-backend
    tier: integration-orchestrator-backend
    track: stable
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
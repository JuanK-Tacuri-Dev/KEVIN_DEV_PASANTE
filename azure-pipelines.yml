# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- develop

resources:
- repo: self

variables:
  tag: 1.0.8 #'$(Build.BuildId)'

stages:
- stage: buildAndPush
  displayName: Build image and Push in ACR
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        containerRegistry: SC_ACR_GRUPOKFC_DEV
        repository: mxpv2-orchestrator
        buildContext: '$(Build.SourcesDirectory)/'
        dockerfile: '$(Build.SourcesDirectory)/Integration.Orchestrator.Backend.Api/Dockerfile'
        tags: |
          $(tag)

    - task: PublishPipelineArtifact@1
      inputs:
        targetPath: '$(Pipeline.Workspace)'
        artifact: 'publisAksMxpv2Orchestrator'
        publishLocation: 'pipeline'
